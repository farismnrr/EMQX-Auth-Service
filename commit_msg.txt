fix: Force GHCR namespace to farismnrr and use arrays for flags

- Hardcoded GHCR_NAMESPACE=farismnrr in autobuild.sh to prevent env overrides.
- Refactored BUILD_FLAGS string to BUILD_ARGS array to handle labels/annotations safely.
- Removed risky 'eval' call, fixing the strange linker errors caused by quote stripping.
