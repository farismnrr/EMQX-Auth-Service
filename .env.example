# =============================================================================
# EMQX Auth Service Configuration
# =============================================================================
LOG_LEVEL=info
SECRET_KEY=
API_KEY=

# =============================================================================
# JWT Configuration (for EMQX Authentication)
# =============================================================================
# Must match EMQX_JWT_SECRET in docker-compose
JWT_SECRET=

# =============================================================================
# MySQL Configuration
# =============================================================================
MYSQL_HOST=
MYSQL_PORT=
MYSQL_DATABASE=
MYSQL_USER=
MYSQL_PASSWORD=
MYSQL_ROOT_PASSWORD=

# =============================================================================
# EMQX Broker Configuration (Local Development)
# =============================================================================
# For local dev with docker-compose
EMQX_HOST=
EMQX_MQTT_PORT=
EMQX_WS_PORT=

# =============================================================================
# MQTT Admin API Configuration
# =============================================================================
MQTT_ADMIN_ENABLED=
MQTT_BROKER_HOST=
MQTT_BROKER_PORT=
MQTT_USE_TLS=
MQTT_ADMIN_USERNAME=
MQTT_ADMIN_PASSWORD=

# Shared Subscription (for multi-instance deployment)
# When enabled, only one instance receives each message (load balancing)
# Format: $share/{group_id}/{topic}
MQTT_USE_SHARED_SUB=

# =============================================================================
# Password Encryption (AES-256-GCM)
# =============================================================================
# Generate with: openssl rand -hex 32
# Must be 64 hex characters (32 bytes)
MQTT_PASS_ENCRYPTION_KEY=